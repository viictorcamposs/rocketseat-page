{% extends "layout.njk" %}

{% block head %}
  <title>Bootcamp</title>
  <link rel="stylesheet" href="/bootcamp-page.css">
{% endblock %}
{% block header %}
  <img src="https://skylab.rocketseat.com.br/static/media/logo.0ae02df1.svg" alt="logo">
  <div class="links">
    <a href="/">Rocketseat</a>
    <a href="/bootcamp-page" class="selected-page">Bootcamp</a>
    <a href="https://discordapp.com/invite/gCRAFhc" target="_blank">Discord</a>
    <a class="contato">Contato</a>
  </div>
{% endblock %}
{% block content %}
  <section class="cards">
    {% for item in items %}
      <div class="card {{ item.color }}" id="{{ item.id }}">
        <div class="inside-card">
          <div class="card-top">
            <span class="{{ item.id }}">{{ item.number }}</span>
          </div>
          <div class="card-content">
            <img src="{{ item.image }}" alt="Curso {{ item.id }}">
          </div>
          <div class="card-info">
            <p class="paragraph"><strong class="{{ item.id }}">{{ item.strongDescription }}</strong> {{ item.description }}</p>
          </div>
        </div>
      </div>
    {% endfor %}
  </section>
{% endblock %}
{% block script %}
  <script src="/bootcamp-page.js"></script>
{% endblock %}